#include<stdio.h>
#define size 5
int top=-1;
int main(){
    if(top>=size){
        printf("stack full");
    }
    else{
int stack(int item(size));
    }
push(10);

top++;
return 0;
}
